apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ml-service
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ml-service
                port:
                  number: 80