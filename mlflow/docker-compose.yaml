version: '3.8'

services:
  db:
    image: postgres:15
    env_file:
      - .env
    ports:
      - "5432:5432"

  mlflow:
    build: .
    env_file:
      - .env
    ports:
      - "5001:5000"
    depends_on:
      - db